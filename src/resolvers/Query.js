import { users, user, searchUsers, usersFeed, currentUser, usernameAvailable } from './users';

export const Query = {
  users,
  user,
  searchUsers,
  usersFeed,
  currentUser,
  usernameAvailable
}

export default Query;